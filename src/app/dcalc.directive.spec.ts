import { DcalcDirective } from './dcalc.directive';

describe('DcalcDirective', () => {
  it('should create an instance', () => {
    const directive = new DcalcDirective();
    expect(directive).toBeTruthy();
  });
});
